<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permisos necesarios para el Gestor de Archivos -->
    <!-- Permiso para leer almacenamiento externo (Android 12 y anteriores) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />

    <!-- Permiso para escribir en almacenamiento externo (Android 12 y anteriores) -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="32"
        tools:ignore="ScopedStorage" />

    <!-- Permiso para gestionar todo el almacenamiento (Android 11+) -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE"
        tools:ignore="ScopedStorage" />

    <!-- Permisos para Android 13+ (acceso granular a medios) -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <application
        android:name=".FileManagerApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FileManager"
        android:requestLegacyExternalStorage="true"
        tools:targetApi="31">

        <!-- Activity Principal - Punto de entrada de la aplicación -->
        <activity
            android:name=".presentation.main.MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Activity para Exploración de Archivos -->
        <activity
            android:name=".presentation.fileexplorer.FileExplorerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.main.MainActivity"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.main.MainActivity" />
        </activity>

        <!-- Activity para Visualización de Archivos -->
        <activity
            android:name=".presentation.fileviewer.FileViewerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.fileexplorer.FileExplorerActivity"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.fileexplorer.FileExplorerActivity" />

            <!-- Intent filter para abrir archivos desde otras aplicaciones -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/*" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity>

        <!-- Activity para Visualización de Imágenes -->
        <activity
            android:name=".presentation.imageviewer.ImageViewerActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.fileexplorer.FileExplorerActivity"
            android:theme="@style/Theme.FileManager.FullScreen">
        </activity>

        <!-- Activity para Configuración y Temas -->
        <activity
            android:name=".presentation.settings.SettingsActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.main.MainActivity"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.main.MainActivity" />
        </activity>

        <!-- Activity para Favoritos -->
        <activity
            android:name=".presentation.favorites.FavoritesActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.main.MainActivity"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.main.MainActivity" />
        </activity>

        <!-- Activity para Historial de Archivos Recientes -->
        <activity
            android:name=".presentation.recent.RecentFilesActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:parentActivityName=".presentation.main.MainActivity"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.main.MainActivity" />
        </activity>

        <!-- Activity para Búsqueda de Archivos -->
        <activity
            android:name=".presentation.search.SearchActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize"
            android:launchMode="singleTop"
            android:theme="@style/Theme.FileManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.SEARCH" />
            </intent-filter>
            <meta-data
                android:name="android.app.searchable"
                android:resource="@xml/searchable" />
        </activity>

        <!-- FileProvider para compartir archivos de forma segura -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

    </application>

</manifest>